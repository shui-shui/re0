# lambda 参数（=>0) :表达式



结果 = list(map(平方函数, 数字列表))

偶数 = list(filter(lambda 当前数字: 当前数字 % 2 == 0, 数字列表))

总和 = reduce(lambda 累加器, 当前数字: 累加器 + 当前数字, 数字列表, 0)【累加器初始值为0

结果是一个迭代器（就是每次只输出一个，都输出完才会报错）

format() 就是往字符串里填东西的工具：result = "我叫{}，今年{}岁".format(name, age)
