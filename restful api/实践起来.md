###### 跟着打：

import requests

url=""

response=request.get(url)

print(response.status\_code)#HTTP 状态码，这次请求成功了吗？

print(response.json())#返回的具体数据，.json() 会把 JSON 转成 Python 字典：



import requests

url="https://jsonplaceholder.typicode.com/users"

data = {

&#x20;   "name": "李四",

&#x20;   "email": "lisi@example.com"

}

response=requests.post(url,json=data)#json=data 会自动帮你加：Content-Type: application/json我发送的数据是什么格式,也不能data=json





print(response.status\_code)

print(response.json())

headers={

&#x09;"Authorization": "Bearer your\_token\_here"

}#这是token是“身份认证”

###### 状态码

状态码	含义

200	成功

201	创建成功（POST）

400	请求错误

401	未授权

404	找不到，资源没找到

500	服务器错误



##### 题目：

**requests.post(url, data=data) 不加 JSON**

后端期望 JSON 格式

你只传普通字符串（或者 form-data），服务器不知道这是 JSON

会返回 400 错误 或解析失败

**Authorization: Bearer xxx**

Authorization = 身份凭证

后端通过 token 识别你是谁、是否有权限访问接口







if response.status\_code == 200:--->if response.status\_code in \[200, 201]:

&#x20;   # 获取返回的数据

&#x20;   data = response.json()

&#x20;   print("用户信息:", data)

else:

&#x20;   print("接口调用失败，状态码 XXX")------->print(f"接口调用失败，状态码 {response.status\_code}")





##### 大挑战：

查看用户信息（GET）

创建新用户（POST）

根据状态码判断请求是否成功

模拟 Authorization token





题目：
功能：
查看用户信息（GET）
创建新用户（POST）
根据状态码判断请求是否成功
模拟 Authorization token
如果你完成上面代码，我给你几个挑战：
让用户入 ID，动态获取用户信息
用循环实现连续创建多个用户
模拟错误场景（比如 404 / 401）并捕获
